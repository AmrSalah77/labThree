package ATM;
import java.util.Scanner;

public class ATM extends withdrawal {
    public static Scanner scanner =new Scanner(System.in);
    public int withdraw(int balance, int withdrawal){
        return stub_withdrawal(balance,withdrawal);
    }
}

